# Schemas

This directory is reserved for canonical data contracts.

Nothing in this bootstrap step is implementation-specific. Future additions
should keep schemas portable across runtimes and storage choices.
