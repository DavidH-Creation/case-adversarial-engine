# Schemas

这里是仓库的 machine-readable contracts。

## Layout

- `case/`
  案件对象和 workspace 相关 schema。
- `procedure/`
  `Run`、`Job` 和程序状态相关 schema。
- `reporting/`
  report artifact、interaction turn 等输出层 schema。
- `indexing.schema.json`
  共享索引结构。

所有 schema 命名都应服从 `docs/03_case_object_model.md`。
