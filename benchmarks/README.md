# Benchmarks

这里存放评测和回归基线，不是示例展示目录。

## Layout

- `civil_loans/`
  当前最成熟的金标集。
- `fixtures/`
  稳定 JSON fixtures 和 replay 示例。
- `acceptance/`
  机器可读的 acceptance 参考。

## Usage

- 用来验证对象模型和输出是否稳定
- 用来比较版本回归
- 用来支撑 acceptance 和 replay 测试
