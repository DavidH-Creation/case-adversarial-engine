name: v0-5-benchmark-contract
task_id: v0-5-benchmark-contract
description: Create the benchmark folder contract and template fixtures for v0.5 civil loan cases without adding model-specific evaluation code.
risk_level: medium
version: "1.0"
acceptance_criteria:
  - Define a repeatable benchmark layout for civil loan cases.
  - Add fixture templates covering materials, issues, evidence, and expected report references.
  - Keep benchmark fields aligned with docs/04_eval_and_acceptance.md.
allowed_paths:
  - "benchmarks/**"
  - "docs/04_eval_and_acceptance.md"
  - "plans/current_plan.md"
forbidden_paths:
  - ".github/**"
  - ".bulwark/policies/**"
  - "docs/00_north_star.md"
  - "docs/01_product_roadmap.md"
  - "ui/**"
timeout_seconds: 300
stop_conditions:
  max_files_changed: 10
  max_total_lines: 500
