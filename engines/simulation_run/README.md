# Simulation Run Engine

这是当前 civil kernel 的分析核心。

目录内的能力包括：

- 对抗推演和回合运行
- 裁判路径树
- 最优攻击链
- 争点影响排序
- 争点分类
- 证据缺口 ROI
- 可信度和 defense-chain 等辅助分析
- `what-if / scenario` 运行与 diff

如果 `Core Track = v2` 在继续长，主要就是在这里和 `shared/` 一起长。
